#include<stdio.h>
 int main()
  {
	int size,i;
	 printf("enter the size of array\n");
	 scanf("%d",&size);
	float arr[size]={1.2,2.1,3.4};
	for(i=0;i<size;i++)
	{
	  scanf("%f\n",&arr[size]);
	 printf("entred number is:%f\n",arr[size]);
       }
	for(i=0;i<size;i++)
	printf("numbers are:%f\n",arr[size]);
   }
